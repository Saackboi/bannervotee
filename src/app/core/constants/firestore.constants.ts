export const FIRESTORE_COLLECTIONS = {
  banners: 'banners',
  votes: 'votes',
  settings: 'settings',
  admins: 'admins',
} as const;

export const FIRESTORE_DOCUMENTS = {
  mainSettings: 'main',
} as const;
